# Development API URL
# In development, the frontend will call the backend at this URL
VITE_API_URL=http://localhost:8000/api

# In production (served by Nginx), the config.js automatically uses relative paths
# No need to set VITE_API_URL in production
